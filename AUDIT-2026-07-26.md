# 機能・UX監査レポート（2026-07-26 朝）

「戻るボタンが無かった」のと同じ種類の**機能欠落**を全画面で洗い直した結果です。
コードを読んだうえで実機（ブラウザ）で1つずつ再現確認しています。

- **修正して公開済み** = 判断が不要な明確なバグ・欠落（SW v26）
- **要判断** = 方針を決めてもらいたいもの（朝の確認事項）

---

## A. 修正して公開済み（v26）

### A-1. 【重大】記録が消えていた
進行中のワークアウトを残したまま新しく開始すると、**前のセッションが完全に到達不能**になっていました。

実際に再現した内容：
1. ベンチプレス 100kg×10 を記録（ホームに 1000kg と表示）
2. ホームの「＋ 空で開始」をタップ
3. → **1000kg が画面上から消滅**（ホームは 0kg に戻る）

原因は、進行中セッションの取得が「未完了のうち最新の1件だけ」で、履歴一覧は「完了したものだけ」を表示していたため、古い未完了セッションがどちらにも出てこなくなっていたことです。データ自体は端末内に残っているのに、UIからは開く手段がありませんでした。

**修正内容**
- 新規開始時に進行中があれば選択ダイアログを出す（進行中を開く／保存して新規／破棄して新規）
- 履歴に**「未完了のワークアウト」欄**を追加 → すでに迷子になっている記録もタップで復帰できる

> 補足：もし今までに「記録したのに消えた」ことがあれば、履歴タブの「未完了のワークアウト」を確認してください。残っていれば復旧できます。

### A-2. 記録中は設定に入れなかった
記録中はホーム右上の歯車が「● 記録中」に置き換わる作りで、設定タブも無いため、**トレーニング中は設定に到達する手段がゼロ**でした。→ 歯車を常時表示に変更。

### A-3. 目標値（達成リング）を変更できなかった
ホームの主役である達成リングは「1日 5000kg」を100%として計算していますが、**この値を変える画面がどこにも存在しませんでした**。→ 設定に「1日の目標」を追加。

### A-4. Androidの戻る操作でアプリが即終了
シートを開いていても、記録画面にいても、戻る操作で**アプリごと終了**していました。→ 「シート → サブ画面 → 記録タブ」の順に1段ずつ戻るように変更。あわせて、記録画面を閉じても内部状態が残り戻る操作が一周してしまう不具合も修正。

### A-5. 閉じるボタンが無いシートが4つ残っていた
今回ご指摘の「種目を選ぶ」と同じ問題です。背景タップに気付かないと詰まります。
→ RM計算機／⋯種目メニュー／カスタム種目追加／日別リスト に追加（カスタム種目追加の「戻る」は種目ピッカーへ復帰）。

### A-6. テンプレート編集の未保存内容が黙って消えていた
名前と種目を入れた後に戻ると、確認なしで破棄。→ 確認を挟むように変更。

### A-7. 「破棄」ボタンが2行に折り返して潰れていた
`.row` 内のボタンが `width:100%` 同士でぶつかり、狭い方の文字が折り返していました（全画面幅で発生）。→ 伸縮ルールを明示して解消。

### A-8. 英語設定でもホーム画面が日本語のままだった
言語を English にしても、ホーム画面の約20箇所（挨拶・目標達成・連続日・完了・今日のメニュー・セットを記録する など）が日本語のまま。→ 全てi18n化。

---

## B. 要判断だった項目 → 回答を受けて対応済み（2026-07-27 / v27）

### B-1. ライトテーマ → ✅ ダーク固定＋テーマ2色
ライトテーマを廃止し**ダーク固定**に。設定から**アクセント色（ライム / オレンジ）**を選べるようにしました（色見本つき）。黒の色味もアクセントに合わせて寒色/暖色へ寄せています。

**あわせて「緑の文字が見にくい」を修正**：ライム背景に白文字を置いていた箇所のコントラスト比が **1.18**（WCAG基準4.5の1/4以下）しかなく、実質判読不能でした。

| 箇所 | 修正前 | 修正後 |
|---|---|---|
| セット完了の ✓ | 1.18 | **16.39** |
| レストタイマーの文字 | 1.18 | **16.39** |
| +30s / スキップ | 1.00 | **16.39** |

全画面をプログラムで実測し、両テーマとも基準未満はゼロです。

### B-2. 過去の日付 → ✅ 記録漏れを救済できるように
1. **カレンダーの空いている日をタップ →「＋ この日に記録を追加」**（記録し忘れた日を後から埋められる）
2. **記録画面の日付を編集可能に**（過去分の修正・日付の付け替え）
3. **体調（食事・睡眠）にも日付欄**を追加
4. 種目を入れずに離れた場合は空の記録を自動で掃除

### B-3. 保存済みワークアウトの種目削除
未対応。現在は履歴 →「編集・再開」→ 🗑 → 保存 の手順です。履歴の詳細画面から直接消せるようにするかは要判断のまま。

### B-4. 種目解説の英語化 → ✅ 完了
- 「鍛える筋肉」30パターン、「フォームのポイント」30パターン、**イラスト内の筋肉ラベル24語**を英訳
- **初期種目の英語名を56件追加**（全88種目が英語名を持つ状態に）。器具・サブ部位も英訳
- 既存ユーザーにも反映されるよう移行処理を追加（SEED_VERSION=4）
- "1 exercises" のような複数形の崩れ、英文中に混ざる「・」も解消
- 88種目すべての解説画面を自動巡回し、**日本語の残りゼロ**を確認

### B-5. 以前からの保留事項
- **アプリ名**：まだ仮の「筋トレ記録」。App Store を見据えると早めに決めたい
- **アイコン**：デザイン案の採用可否
- **無料/有料の線引き**（フリーミアム設計）
- **UI第2弾**：記録入力・グラフ・履歴の再デザイン（2a/2b/2c）※着手待ち

---

## C. 確認したが問題なかったもの

- 種目の追加・削除・並び替え、セット追加/削除/完了、ウォームアップ
- 総重量・セット数・グラフ・ホーム合計の再計算（削除も即反映）
- 有酸素種目の時間/距離入力
- テンプレートの作成→開始→セッション生成
- 体調記録の保存・再編集（睡眠の星の復元含む）
- 単位 kg/lbs 換算、JSONの書き出し/読み込み
- 長い種目名でのレイアウト（横スクロール発生なし）
- 初回起動（データ空）からの一連の流れ、コンソールエラーなし
